// Set the real receiving address when the contact channel is approved.
// With an empty address, the form only offers a local preview and copy action.
window.MAM_CONFIG = Object.freeze({ contactEmail: '' });
